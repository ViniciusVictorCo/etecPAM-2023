import { StyleSheet } from "react-native";

const style = StyleSheet.create({

    container:{
        flex: 1,
        backgroundColor: '#fff',
        alignItems: "center"
    },
    viewHeader:{
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 8,
        marginLeft: 10
    },
    textHeader:{
        fontSize: 25,
        color: '#000',
        fontWeight: 'bold',
        marginRight: 20,
        marginLeft: 225
    }

})

export default style;